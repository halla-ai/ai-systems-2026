# Sample API

## Functions

`parse_json(data: str) -> dict`

Parses JSON string and returns a dictionary.

`greet(name: str, loud: bool = False) -> str`

Returns a greeting message.

`fetch_items(limit: int) -> list[dict]`

Fetches items from storage.
